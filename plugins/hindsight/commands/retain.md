---
description: Save information to Hindsight memory bank
allowed-tools: ["Bash"]
argument-hint: [information to save]
---

The user wants to save the following to the memory bank:

$ARGUMENTS

Follow the retain skill instructions: choose category, format content with WHAT + WHY, execute via do-retain.sh with heredoc.

After saving, confirm what was stored and the category.
